package com.Object.CommonClass.WrapperClass;

public class statement {
    /*
        在Java中8种基本数据类型不属于类，不具备“对象”的特征，没有成员变量和方法，不方便进行面向对象的操作。
        为此，Java提供包装类（Wrapper Class）来将基本数据类型包装成类，
        每个Java基本数据类型在java.lang包中都有一个相应的包装类，每个包装类对象封装一个基本数据类型数值。
        对应关系如下所示，除int和char类型外，其他的类型对应规则就是第一个字母大写。

        基本数据类型      包装类
        boolean         Boolean
        byte            Byte
        char            Character
        short           Short
        int             Integer
        long            Long
        float           Float
        double          Double

        包装类都是final的，不能被继承。包装类都是不可变类，类似于String类，
        一旦创建了对象，其内容就不可以修改。包装类还可以分成三种不同类别：
            数值包装类（Byte、Short、Integer、Long、Float和Double）
            Character
            Boolean
    */
}
